buildscript {
    repositories {
        google()
        mavenCentral()
    }
    // dependencies {
    //     classpath 'com.android.tools.build:gradle:8.2.1'
    //     classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.10"
    //     classpath 'com.google.gms:google-services:4.4.1'
    // }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
    tasks.withType(JavaCompile) {
        options.fork = true
        options.forkOptions.executable = "C:\\Users\\brita\\Downloads\\jdk-17.0.12_windows-x64_bin\\jdk-17.0.12\\bin\\java.exe"  // Caminho para o seu JDK 17
    }
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
    